repositories {
    maven { url = uri('https://repo.viaversion.com') }
}

dependencies {
    compileOnly('com.viaversion:nbt:5.2.0') { transitive = false }
}

javadoc {
    options {
        links 'https://jd.viaversion.com/'
    }
}