repositories {
    maven { url = uri('https://repo.viaversion.com') }
}

dependencies {
    compileOnly('com.viaversion:nbt:5.1.2') { transitive = false }
}

javadoc {
    options {
        links 'https://jd.viaversion.com/'
    }
}